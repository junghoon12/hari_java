/*
          - animate() : 선택한 요소에 다양한 동작(motion) 효과를 적용하는 함수.
            형식) $("요소선택").animate({애니메이션 동작}, 효과속도(ms), 콜백함수);
            ==> 애니메이션 속성에는 모션으로 적용할 속성을 CSS(스타일)을 이용해서 입력함.
                효과 속도는 ms(1/1000) 또는 "slow", "normal", "fast" 로도 
                입력이 가능함.
        */
