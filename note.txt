/*
          - fadeIn() : 숨겨져 있던 요소가 점점 불투명해 지면서 노출이 되는 함수.
            형식) $("요소선택").fadeIn(효과속도(ms), 콜백함수);
            
          - fadeOut() : 노출이 되어 있던 요소가 점점 투명해지면서 사라지는 함수.
            형식) $("요소선택").fadeOut(효과속도(ms), 콜백함수);
        
          - fadeToggle() : 노출이 되어 있던 요소는 점점 사라지고, 사라져 있던 요소는
                        점점 선명해 지는 함수.
            형식) $("요소선택").fadeToggle(효과속도(ms), 콜백함수);
        */
